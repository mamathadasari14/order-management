apiversion:apps.v1
kind: deployment
metadata: 
name:123
  name: nginx-deployment
  lables:
    app:
      spec:
        replicas: 3
        selector:i
          matchlables:
            app: nginx
            template:
              metadata:
                lables:
                  app:nginx
                spec:
                  containers:
                    -name: ngixn
                    image: nginx:1.14.12
                    ports:
                      - containerport: 8081



